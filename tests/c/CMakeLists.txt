FILE(GLOB EXAMPLE_SOURCE_FILES *.c)

FOREACH(EXAMPLE_FILE ${EXAMPLE_SOURCE_FILES})
	GET_FILENAME_COMPONENT(EXAMPLE "${EXAMPLE_FILE}" NAME_WE)

	ADD_EXECUTABLE("cairocks-test-${EXAMPLE}" "${EXAMPLE_FILE}")
	TARGET_LINK_LIBRARIES("cairocks-test-${EXAMPLE}" cairocks)
ENDFOREACH(EXAMPLE_FILE)

